import { DefaultScene } from "./default-scene";

export class PhysicsScene extends DefaultScene {
  /**
   * Here you should put methods and 
   * props you will only use in 
   * physics scenes.
   * for example a method for change
   * gravity:
   * 
   * changeGravity(newGravity: number) {
   *   this.physics.world.gravity.y = newGravity;
   * }
   * 
   * 
   * or the max velocity for all
   * your game objects:
   * 
   * maxVel = 300;
   * 
   */
}