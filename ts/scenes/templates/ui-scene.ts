import { DefaultScene } from "./default-scene";

export class UIScene extends DefaultScene {
  /**
   * Here you should put methods and 
   * props you will only use in 
   * ui scenes (like a menu scene
   * or a loose scene).
   * for example a method for change
   * scene:
   * 
   * goTo(scene: string) {
   *   this.scene.pause().run(scene)
   * }
   * 
   * 
   * or an array with a list values:
   * 
   * menuList = [
   *   "restart level",
   *   "main menu"
   * ];
   * 
   */
}