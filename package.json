{
    "scripts": {
        "build": "tsc -p \"./tsconfig.json\"",
        "open": "start http://127.0.0.1:3000/ || open http://127.0.0.1:3000/",
        "start": "npm run open && clear && http-server -p 3000"
    },
    "dependencies": {
        "http-server": "^0.12.3",
        "phaser": "^3.54.0",
        "requirejs": "^2.3.6",
        "tslint": "^6.1.3"
    },
    "devDependencies": {
        "typescript": "^4.2.4"
    }
}